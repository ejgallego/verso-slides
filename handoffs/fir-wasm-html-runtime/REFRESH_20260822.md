# FIR complete-HTML artifact refresh after VERSO-001 — completed

## Outcome

W7 completed thread `VERSO-W7-20260822-003` and landed the package
machinery on FIR `main` at `d124340e`. The immutable local package is:

```text
/home/egallego/lean/fir/.worktrees/wasm-verso-html-refresh/integration/verso-html/_build/verso-html-packages/c88e4a543fb2-970b071b73ad-1db32e18a6a157876b95
```

It records FIR `c88e4a543fb2553ab07a3419b1ce038e1519f083`, Verso
`970b071b73adc6e68c6de00bc183460f76d97731`, Lean 4.33, and a
155,103-byte zero-import Wasm with SHA-256
`81b7bf1b6a62ecdd6723c24600020058ab6c578d47e9d9bcb61d6517a1c48d45`.
The ABI and output schema are unchanged. W7's native/Wasm
differential, bounded-growth, repeat, malformed-input, real-Chrome,
`make check`, Talos, and artifact checks all passed.

## Request

Rebuild and atomically publish the accepted FIR complete-HTML browser
package from Verso Slides commit
`970b071b73adc6e68c6de00bc183460f76d97731` on the pushed fork branch
`ejgallego/feat/vir-pretty-prototype`.

The exact compiler-neutral source is:

```text
VersoSlides/Pretty.lean
SHA-256 f4a9c956a78a5781b4a6963059e157c35dc20fc14b0081350a280a162ac7464d
```

It has been checked directly with Lean 4.33. The entrypoint and
artifact contract remain unchanged:

```lean
VersoSlides.Pretty.formatHtmlForRuntime
  (f : Std.Format)
  (annotations : Array VersoSlides.Pretty.TaggedAnnotation)
  (width indent column : Nat) : String
```

- browser API: `fir.prettyM.html.browser/v1`
- output schema: `verso-token-html/v1`
- package contract:
  `demos/vir-pretty/contracts/fir-native-html-v1.json`
- bounded artifact toolchain: Lean 4.33

## Why this is a rebuild

Before this refresh, the staged package recorded Verso commit
`eb8d2b8f…` and `VersoSlides/Pretty.lean` digest `bf4271d6…`. It
therefore cannot contain the VERSO-001 lookup change.

The new source detects ascending annotation tables once and uses
upper-bound binary search while preserving the existing reverse-scan
fallback for unsorted input and last-match behavior for duplicate
tags. The ABI and HTML semantics do not change. On VIR, two
interleaved same-runtime campaigns reduced the 256-annotation execute
phase by 46.8–49.6%. FIR must be rebuilt before we can measure how
much of its existing 12.290-ms result came from the same source
algorithm.

## Producer acceptance

1. Preserve all existing package, native-oracle, Wasm differential,
   ownership, import/export, large-input, repeated-call, and
   atomic-publication gates.
2. Add or retain explicit cases for ascending, unsorted, and
   duplicate-tag annotation tables.
3. Record clean Verso/FIR/toolchain provenance and the exact
   relevant-file digest above.
4. Publish a new immutable package; do not mutate the old accepted
   package.
5. Reply with the immutable path, source/FIR/toolchain identities,
   Wasm digest and bytes, package smoke result, and any contract
   deviation.

## Consumer acceptance after publication

Verso will validate and copy only the checksummed browser subset into
its own workspace, rebuild the lab, run the full backend × breadth
smoke, and repeat:

- the two interleaved 256-annotation campaigns;
- the post-fix JS/FIR/VIR three-way focused comparison; and
- the 58-format × widths 20/40/80 representative corpus.

The durable evidence is
[`VERSO-001`](../../performance-cards/pretty/VERSO-001-annotation-lookup.md).
Coordination thread: `VERSO-W7-20260822-003` in the FIR canonical
mailbox.

Completed on 2026-08-24:

- the checksummed subset validates and is staged in the workspace;
- the backend × breadth browser smoke and production panel smoke pass;
- the full Chromium suite passes 230/230;
- the 58-format × widths 20/40/80 rehearsal passes 1,218 samples per
  backend;
- two same-harness old/new FIR pairs reduce the 256-annotation execute
  median from 25.505 to 3.485 ms and from 25.530 to 5.155 ms
  (79.8–86.3%); and
- committed medians fall from 30.710 to 6.255 ms and from 31.960 to
  9.550 ms (70.1–79.6%).

The paired raw-report SHA-256 values are `4f4eb0ea…`/`0a00d73a…` and
`12664f6b…`/`c60ba9cd…` for old/new respectively. These browser
observations confirm that the shared Lean source fix reached FIR; they
are not instruction- count or compiler-only measurements.
